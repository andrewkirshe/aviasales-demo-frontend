import React from "react";
import styled from "styled-components";

const Wrapper = styled.div`
  background: #fff;
  border-radius: 4px;
  padding: 20px;
`;

export default props => {
  return <Wrapper>Фильтр</Wrapper>;
};
